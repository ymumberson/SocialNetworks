using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClusteringTests : MonoBehaviour
{
    [SerializeField] public GraphRendererScript gr;
    [SerializeField] public Agent a1;
    [SerializeField] public Agent a2;
    [SerializeField] public Agent a3;
    [SerializeField] public Agent a4;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
